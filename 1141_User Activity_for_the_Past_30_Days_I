# Write your MySQL query statement below

select activity_date as day, count(distinct(user_id)) as active_users
from Activity
where abs(datediff(activity_date, '2019-07-27')) <= 30
group by activity_date
